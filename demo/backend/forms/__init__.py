"""작성 서식의 PDF 내보내기 (P7 / R9).

원본 서식 PDF를 템플릿으로 삼고, 입력값을 좌표에 얹는 방식이다(설계 6-B.1의 A안).
결과물이 종이 서식과 구분되지 않아야 "행정 서식을 디지털로 옮겨도 서류로서
성립한다"는 이 데모의 논점이 선다.

  coords.py  — `fixtures/form_coords/*.json` 좌표 맵 읽기
  values.py  — 신청 1건 → 서식 칸에 넣을 값
  render.py  — PyMuPDF 오버레이 (글자 · ✓ · 전자서명 이미지)

**메인 기능이 아니다.** 미구현이어도 DoD에 영향이 없는 선택 항목이라,
좌표 맵이 없거나 템플릿이 없으면 예외 대신 "내보낼 수 없음"으로 조용히 빠진다.
"""
