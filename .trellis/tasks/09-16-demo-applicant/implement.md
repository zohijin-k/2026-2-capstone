# 실행 계획 — 신청자 화면 (P0 ~ P3)

부모 태스크 `09-16-demo-site/implement.md`에서 이 태스크가 맡은 Phase만 떼어낸 것이다.
설계 근거는 부모 `design.md`, 업무 규칙 전문은 `docs/demo-site-dev-plan.md` 2절을 직접 Read 할 것.

> ★ **작업 영역 규칙**
> 생성하는 모든 파일은 **`demo/` 폴더 안에만** 둔다.
> 저장소 루트·`engine/`·`dashboard/`에는 파일을 추가하거나 수정하지 않는다.
> 허용 예외는 둘: 루트 `.gitignore` 항목 추가, `docs/demo-site-dev-plan.md` 갱신.
> 아래 경로는 별도 표기가 없으면 전부 `demo/` 기준 상대경로다.

---

## P0. 정렬 및 스캐폴딩 (D0 ~ D+1)

- [x] `docs/demo-site-dev-plan.md`를 정식 프로젝트명·PDF 검증 결과로 전면 개정 → **진·채운에게 공유**
- [ ] `design.md` 9절(E1~E13) 엔진 수정 요청을 진에게 전달, `doc_types` 단일 소스 합의 ⏸ **사람이 해야 함** (문서는 준비됨: `docs/demo-site-dev-plan.md` 5절)
- [ ] 채운과 화면 경계 확정 (데모=건별 심사 / 대시보드=집계) ⏸ **사람이 해야 함**
- [x] **`demo/` 폴더 생성** — 이후 모든 산출물의 루트
- [x] `demo/backend/` FastAPI 스캐폴딩 (`main.py`, `requirements.txt`) — 루트에 `requirements.txt`를 만들지 않는다
- [x] `demo/frontend/` Vite 스캐폴딩 (`package.json`, `vite.config.ts`) — 루트/`dashboard`의 `package.json`과 분리
- [x] `demo/backend/rules/programs.py`, `rules/doc_types.py` 상수 작성 (PDF 원문값 그대로)
- [x] `demo/backend/engine_adapter.py` 골격 — `sys.path`에 저장소 루트 추가 후 `from engine.pipeline import run_pipeline`. **`engine/`을 복사하지 않는다**
- [x] 루트 `.gitignore`에 4줄 추가: `demo/storage/`, `demo/**/*.db`, `demo/frontend/node_modules/`, `demo/frontend/dist/`

**게이트**: ✅ **통과 (2026-09-16)**
- `uvicorn demo.backend.main:app --port 8000` + `npm run dev`(5173) 동시 기동 확인
- `/api/programs`가 두 사업 설정 반환 (두배적금 scored/보완불가/정원1300, 취업패키지 first_come/7일보완)
- vite 프록시로 `localhost:5173/api/programs` 도달 확인
- `tsc -b` · `oxlint` 통과
- `git status`에 `demo/` 외 신규 파일 없음 (`.gitignore`만 2줄 추가)
- 엔진 런타임 오버라이드 확인: 필수서류가 **주민등록초본**으로 교체됨, cutoff 2026-03-03 / 2026-01-01, 혼동군에 `사업자등록증 ↔ 증명` 추가

---

## P1. 신청 골격 — 수기 0건 + 서식 원형 재현 (D+1 ~ D+5)

- [x] `POST /api/applications` + `PATCH` 부분 저장 + 임시저장 복구 (R3.4)

**P1-a. `FormSheet` 컴포넌트 기반 작업 (R8)** — 이걸 먼저 만들고 나머지를 그 위에 얹는다

- [x] 원본 PDF에서 서식1~6을 **논리 쪽 단위로** 캡처해 재현 기준 확보 (⚠️ 2-up 레이아웃 주의)
- [x] `FormSheet` / `FormSection` / `FormRow` / `FormField` / `FormCheckGroup` 컴포넌트
  - [x] CSS Grid 라벨열+입력열, 테두리·라벨 배경·맑은고딕 스택 (관공서 서식 관습)
  - [x] `□` 체크박스 사각 테두리 스타일 (모양은 서식, 동작은 웹) (R8.3)
  - [x] 각주(`※ ...`) 슬롯 — **원문 그대로** 표시 (R8.2)
  - [x] `derived` 모드 — 자동 선택 + 읽기전용 + 근거 문장 한 줄 (R8.5)
  - [x] **PC 전용 고정 레이아웃** — 열 너비를 원본 PDF 선 좌표에서 추출, 좁은 화면은 가로 스크롤만 (R8.6)
  - [x] 체크박스 줄당 개수를 원본과 동일하게 고정 (3/2/5/흐름)
  - [x] 날짜 년·월·일 3분할 입력 (R8.8)

**P1-b. 서식별 구현**

- [x] 서식2 자가진단 — `점검내용 | 확인·동의` 2열 표 그대로, 예/아니오 라디오, 6번 하위 ⑨개 접이식 (R3.1)
  - [x] "아니오" 발생 즉시 중단 + **부적격 사유 + 대안 안내** (기초생활수급자 → 청년내일저축계좌 등)
- [x] 서식1 참여신청서 — **위저드 스텝 = 서식 절 경계** (R8.4)
  - [x] Step0 저축목적 · 유사 자산형성사업 참여 여부 (서식 상단)
  - [x] Step1 Ⅰ. 기본정보 — 인적사항 · 비상연락망
  - [x] Step2 Ⅰ. 기본정보 — 거주기간(전입일 → **자동 구간 체크**) · 가구 및 소득
  - [x] Step3 Ⅰ. 기본정보 — 근로사항 (근로유형 → 체크리스트 결정, 취업일 → **자동 구간 체크**)
  - [x] Step4 Ⅱ. 납입금액 · Ⅲ. 입금 받을 계좌 · Ⅳ. 기타사항(인지경로 + 동의문 3개)
  - [x] **"서식1 전체 보기" 토글** — 전체를 한 장으로 펼침
- [x] 서식3 개인정보 수집·이용 — 고지문 **원문 그대로** + 동의 체크 2개(일반/고유식별정보) (R1.1)
- [x] 서식4 제3자 제공 — 고지문 원문 + 동의 체크 1개
- [x] 서식5 행정정보 공동이용 — 표 그대로 + 서명란에 **캔버스 전자서명** + "현행 지침은 자필서명 — 전자서명 전환 제안" 배지 + 자필 업로드 토글 (R1.2)
- [x] `consent` 테이블에 동의 시각·IP·UA 기록

**게이트**: ✅ **통과 (2026-09-16)**
- 파일 업로드 0건으로 [자가진단]→[신청서 3스텝]→[동의 3종+전자서명] 완주
- 백엔드 E2E 확인: 신청 생성(DS-2026-000001) / 자가진단 부적격·적격·미완료 3경로 / 부분저장 복구 / 동의 4건 + 서명 PNG 저장 + 시각·IP·UA 기록
- 구간 자동판정 경계값 검산: 전입 2025-03-03→"1년 이상", 2025-03-04→"1년 미만", 2022-03-03→"4년 이상~5년 미만" 등 11케이스 일치
- `tsc -b` · `oxlint` 무경고
- 열 너비를 원본 PDF 선 좌표에서 추출해 적용 (6.3 / 17.2 / 32.4 / 49.2 / 66.1 / 84.8 …)

**남은 검수(사람)**: 화면과 PDF 원본을 나란히 놓고 ① 항목 순서 ② 각주 문구 ③ 체크 옵션 개수 대조.

---

## P2. 업로드·즉시 판정 (D+5 ~ D+9)

- [x] `rules/required_docs.py` — 조건부 체크리스트 (R3.2)
- [x] `GET /required-documents` — 근로유형 변경 시 목록이 실제로 바뀜 (+ `PUT /doc-context`로 근로유형 저장)
- [x] `ocr/base.py` — `OcrAdapter` 프로토콜 + `OcrResult` + `BBox`
- [x] `ocr/fixture.py` — Tier1, 시연 13종 고정 응답 (`demo/fixtures/make_samples.py`로 더미 파일 생성)
- [x] `ocr/pdftext.py` — Tier2, PyMuPDF 텍스트+좌표 추출 → 정규식 파싱
  - [x] 문서종류 분류, 발급일, 가구원수, 건보료 고지금액, 전입일, 주소변동내역 유무
  - [x] 암호 PDF 탐지 (`needs_pass`)
  - [x] 화면캡처 휴리스틱 → `NEEDS_REVIEW` (해상도 + EXIF 카메라정보 부재)
- [x] `ocr/upstage.py` — Tier3 스텁 (인터페이스만)
- [x] `POST /documents` — 업로드 → 1단계 판정 → **5초 내 인라인 응답** (R2.1)
- [x] 업로드 UI — 슬롯별 [발급처 바로가기][파일 선택][발급일 직접 입력], 결과 3종 표시 (R2.2)
- [x] 자주 틀리는 것 인라인 경고: 등본≠**초본**, 사업자등록증≠**증명**, PDF 암호 해제, 5년 주소변동내역 포함
- [x] 병합 PDF 페이지 자동 분리·슬롯 배정 (R3.5)

**게이트**: ✅ **통과 (2026-09-16)**
- `python -m demo.backend.tests.run_p2_scenarios` — 48개 검증 항목 전부 통과 (실패 0)
- S3~S6 재현: `WRONG_DOCUMENT_TYPE` / `DOCUMENT_ISSUED_TOO_EARLY` / `DECLARED_DATE_MISMATCH` / `UNREADABLE_FILE`. fixture(Tier1)와 실제 PDF(Tier2) **양쪽에서** 동일하게 나온다
- 추가 재현: S2 `PASS`, S8 `NEEDS_REVIEW`(저신뢰), 주소이력 미포함 `MISSING_ADDRESS_HISTORY`, 화면캡처 `SCREEN_CAPTURE_SUSPECTED`(자동반려 아닌 담당자 확인)
- 근로유형 5종의 체크리스트가 서로 전부 다름 — 직장가입자→4대보험가입내역 / 지역가입자→일용근로내역서 / 사업자→사업자등록증명 / 농업→농업경영체 / 어업→어업경영체. 행정기관 기간제 시 근로계약서, 복수 사업장 시 `work_proof_2` 추가
- Tier2가 더미 PDF에서 문서종류·발급일(2026-03-05)·가구원수(4)·건보료 고지금액(200000)·주소변동내역(포함)을 실제로 추출하고 bbox까지 확보
- 병합 4쪽 pdf → 3쪽 자동 배정(전부 `PASS`) + 미판별 1쪽은 담당자 확인 대상으로 분리
- 판독 소요 13ms (기준 5,000ms)
- `tsc -b` · `oxlint` 무경고 (exit 0)
- `git status`에 `demo/` 외 신규 파일 없음 (`.gitignore` 1줄 추가만)

**남은 검수(사람)**: 실제 정부24·건보공단 발급 PDF로 Tier2 정규식 적중률 확인. 더미 PDF는 문구만 흉내낸 것이라 실제 서식의 줄바꿈·표 구조에서 라벨 패턴이 어긋날 수 있다.

**P2에서 남긴 것**
- `ocr/upstage.py`는 스텁이다 (부모 prd.md C3: 상용 OCR API 비목표). 스캔 이미지 jpg/png는 신뢰도 0 → `NEEDS_REVIEW` → 담당자 큐로 간다. 화면캡처 판별과 화질 추정만 동작한다
- `GET /api/files/{doc_id}` 인라인 스트리밍은 담당자 화면(R4.1) 소관이라 `09-16-demo-officer`로 넘긴다

---

## P3. 통합심사·채점 (D+9 ~ D+12)

- [x] `engine_adapter.py` — 데모 모델 ↔ 엔진 `Applicant`/`Document` 변환 + 런타임 config 오버라이드
- [x] `POST /submit` — 전체 파이프라인 실행 + `review_payload` 저장 + `file_ref` 주입
- [x] `rules/scoring.py` — 심사표 100점 (R5)
  - [x] 중위소득 40점 (140% 표 선형 환산 + `(데모 추정치)` 라벨)
  - [x] 도 거주기간 25점 / 근로기간 25점 (공고일 기준 역산)
  - [x] 연령 10점 (2025-12-31 기준 만나이)
  - [x] 항목별 `basis` + `source_doc` + `bbox` 채우기
  - [x] 동점자 정렬 키
- [x] 제출 전 최종 확인 화면 — 부적합 남으면 제출 버튼 비활성화 + 해당 위치로 이동
- [x] 두배적금에만 "보완 요청이 없는 사업" 강조 경고
- [x] 마이페이지 — 진행 상태 5단계, 제출 서류 판독 결과 재확인, **점수 미노출** (R5.3)

**게이트**: ✅ **통과 (2026-09-16)**
- `python -m demo.backend.tests.run_p3_scenarios` — 109개 검증 항목 전부 통과 (실패 0)
- **S2 수기 검산 일치**: 40(중위소득) + 25(거주) + 25(근로) + 9(연령) = **99점**.
  중위소득 항목 근거 문장이 그대로 저장된다 — "4인 가구 직장가입자, 고지금액 평균 200,000원 ÷ 100% 기준 222,165원 = 중위소득 90.0% → 100% 미만"
- 구간 경계 검산: 222,000원→40 / 222,165원(100.0%)→37 / 244,382원(110%)→34 / 266,598원(120%)→31 / 288,815원(130%)→28
- **S7 부적합**: 340,000원 → 153.0% → 140% 상한 초과 → 엔진 2단계 `INCOME_OVER_THRESHOLD`, 최종 `FAIL`. 1단계(서류)는 `PASS`라 "서류는 멀쩡한데 자격이 안 된다"가 그대로 드러난다. 점수(87)는 담당자에게만 전달된다
- 항목 4개 전부 `basis` + `source_doc` + `source_document_id` 저장. 중위소득·거주기간은 `bbox`까지 (납부확인서 건강보험료 / 초본 전입일)
- `review_payload.documents[].file_ref` 5건 주입 (`/api/files/{id}`) + 전자서명으로 갈음한 서식5가 서류 1건으로 포함됨
- 동점자 정렬: ① 소득 적은 자 ② 거주 긴 자 ③ 근로 긴 자 ④ 연령 낮은 자 — 4단계 모두 정렬 키로 재현
- **마이페이지 점수 미노출**: `/status` 응답 트리에서 금지 키(score·total·band·basis·tiebreak·income_percent·review_payload) 0건, 총점 숫자 미포함, '점수'라는 낱말은 미공개 안내 문장(`result_notice`) 한 곳에만 등장. 프론트 `src/` 전체 문자열 검색에서도 점수 표시 코드 0건
- 제출 전 최종 확인: 빈 신청 건은 18개 항목이 제출을 막고 각각 이동 위치(form1/consent/upload)가 붙는다. 미완료 제출 시도는 400. S3(초본 자리에 등본)을 올린 건은 그 슬롯 1건만 막고 `resident_abstract`로 되돌린다
- P2 회귀: `python -m demo.backend.tests.run_p2_scenarios` 48/48 통과 유지
- `tsc -b` · `oxlint` · `vite build` 무경고 (exit 0)
- `git status`에 `demo/` 외 신규 파일 없음

**P3에서 남긴 것**
- `GET /api/files/{doc_id}` 인라인 스트리밍은 여전히 officer 태스크 소관이다. `file_ref`에 경로만 주입해 두었으므로 그쪽에서 라우트만 붙이면 된다
- 중위소득 100~130% 기준선은 140% 표 선형 환산값이다. 코드 상수(`MEDIAN_INCOME_140`)와 저장된 `score_json.notes` 양쪽에 `(데모 추정치)` 라벨이 붙는다. TF에서 원표를 받으면 상수 표만 교체
- 엔진 `income_decile`(1~10) 스키마 때문에 중위소득 %를 분위로 근사 매핑해 넘긴다(E6·E7). 140% 경계는 정확히 보존되지만 분위 값 자체는 표시용 근사치다
- 재제출·수정 제출 동선은 다루지 않는다(제출 후 `status`가 `submitted`로 잠긴다). 데모 시연에 필요 없고, 실제 정책도 "보완 없음"이다

---

---

## 이 태스크의 시연 시나리오

| # | 시나리오 | 증명하는 것 |
|---|---|---|
| S1 | 자가진단에서 "기초생활수급자" 체크 | 신청 진입 전 차단 + 청년내일저축계좌 안내 |
| S2 | 완전 적합 (4인 직장가입자 건보료 **200,000** / 거주 5년 / 근로 3년 / 27세) | 전 단계 통과 → **40+25+25+9 = 99점**<br>200,000 ÷ 222,165 = 90% → 100% 미만 → 40점 |
| S3 | 초본 자리에 **등본** 업로드 | `WRONG_DOCUMENT_TYPE` — 공고문이 직접 지목한 미비 사유 |
| S4 | 발급일 2026-02-28 초본 | `DOCUMENT_ISSUED_TOO_EARLY` — 공고일 이전 |
| S5 | 입력한 발급일 ≠ 서류상 발급일 | `DECLARED_DATE_MISMATCH` — 오업로드/위변조 이중 방어 |
| S6 | 암호 걸린 PDF | `UNREADABLE_FILE` — 공고문 명시 미비 사유 |
| S7 | 4인 직장가입자 건보료 340,000원 | 140% 상한 311,031원 초과(153%) → 자격 부적합 |
| S8 | 저해상도 사진 (신뢰도 0.6) | `NEEDS_REVIEW` → 담당자 큐로 이관 (확인은 officer 태스크) |

S9(담당자 분할 심사)·S10(취업패키지)은 형제 태스크 소관이다.

---

## 다음 태스크로 넘기는 것

- `review_payload` + `ScoreSheet`(basis·source_doc·bbox 포함)를 DB에 저장 → `09-16-demo-officer`가 읽는다
- `ProgramConfig` 구조체는 P0에서 만들되 취업패키지 값은 `09-16-demo-jobpkg`가 채운다
