# 실행 계획 — 취업지원패키지 분기 및 시연 준비 (P5 ~ P7)

부모 태스크 `09-16-demo-site/implement.md`에서 P5·P6·P7을 떼어낸 것이다.
업무 규칙은 `docs/demo-site-dev-plan.md` 2.6절, 설계는 부모 `design.md` 6(실비 계산)·6-B(PDF 내보내기).

**선행 조건**: `09-16-demo-applicant`(P0~P3)가 끝나 `ProgramConfig` 분기 구조와 업로드·판정 파이프라인이 있어야 한다.

> ★ **작업 영역 규칙**
> 생성하는 모든 파일은 **`demo/` 폴더 안에만** 둔다.
> 저장소 루트·`engine/`·`dashboard/`에는 파일을 추가하거나 수정하지 않는다.
> 허용 예외는 둘: 루트 `.gitignore` 항목 추가, `docs/demo-site-dev-plan.md` 갱신.

---

## P5. 취업지원패키지 (D+16 ~ D+19)

- [x] 사업 선택 → `ProgramConfig` 분기 (연령·인정일·소득요건 없음)
- [x] 자가진단 축소판 (연령·거주 2문항)
- [x] 지원 항목 **복수 선택** UI + 항목별 횟수 선택
- [x] 항목별 추가서류 체크리스트 조립 (면접확인서 / 결제영수증 / 사진사본 / 응시확인서·성적표)
- [x] `rules/subsidy.py` 실비 계산 + 예상 지원금 합계 표시 (R6.2)
- [x] 응시확인서 **응시일 표기 필수** 검증
- [x] **선착순** 표시 + **7일 보완** 안내 및 보완 기한 카운트다운 (R6, E12)

**게이트**: ✅ **통과 (2026-09-16)**
- `python -m demo.backend.tests.run_p5_scenarios` — 146개 검증 항목 전부 통과 (실패 0)
- **S10 재현**: 면접비 2회 + 정장 1회 + 자격증 1회 복수 신청 → 지급 예정액 **193,000원**
  (면접비 50,000×2 정액 + 정장 **70,000원 → 50,000원** + 자격증 43,000원).
  근거 문장이 그대로 저장·표시된다 — "영수증 70,000원 > 한도 50,000원 → 50,000원 지급"
- 실비 경계값: 정장 35,000→35,000 / 50,000→50,000 / 70,000→50,000, 사진 18,000→18,000 / 25,000→**20,000**, 응시료 43,000→43,000 / 60,000→**50,000**. 면접비는 영수증 없이(또는 10,000원이어도) 회당 **50,000원 정액**
- 횟수 상한: 면접비 3회 신청 → 2회로 조정 + "최대 2회" 경고, 사진비 2회 → 1회. 실비 항목은 영수증 금액 미입력 시 `pending`으로 남고 제출이 막힌다
- **사업 분기**가 값으로 갈린다: 연령 기준일 2025-12-31 ↔ 2026-01-01, 출생범위 1986~2007 ↔ 1987~2008, 인정일 2026-03-03 ↔ 2026-01-01, 소득요건 有 ↔ 無, 근로요건 有 ↔ 無, 선발 점수제 ↔ 선착순, 보완 불가 ↔ 7일, 정원 1,300(시군별) ↔ 900(총량)
- **자가진단 8문항 → 2문항**. 취업패키지에서 2문항만 답해도 완료, 같은 답을 두배적금에 넣으면 미완료. 2번 부적격 사유가 취업패키지 기준(1987.1.1.~2008.12.31.)으로 나온다
- **체크리스트 조립**: 항목 미선택 시 공통 3줄(신청서·초본·통장사본) 중 업로드는 초본 1종뿐. S10 선택 시 7슬롯으로 늘고, 사진비만 남기면 3슬롯으로 준다. 신청서=온라인 작성, 통장사본=계좌번호 입력으로 갈음(R1.4)
- **응시일 표기 필수**: 응시일 없는 응시확인서 → `MISSING_REQUIRED_FIELD` **FAIL** + 해결 방법 제시. 응시일 있는 응시확인서·성적표는 둘 다 같은 슬롯에서 `PASS`(택1), 엉뚱한 서류는 `WRONG_DOCUMENT_TYPE` 하나만 뜬다
- **선착순·보완**: 접수 순번 1번/900건(남은 899), 확인필요 서류가 있는 건은 제출 즉시 보완 기한 D-7이 잡히고 보완 대상 서류가 지목된다. 두배적금은 두 블록 모두 `null`
- 점수제가 아니므로 `total_score`는 `None`, 심사표 `{}`. 소득요건이 없어 엔진 2단계도 `PASS`. 마이페이지 응답의 금지 키 0건
- 담당자 화면: 취업패키지는 **접수순** 기본 정렬 + 총량 진행률 배지, 자격요건은 거주지·연령 2항목, 항목별 지급 내역과 보완 기한 카운트다운이 뜬다. 두배적금은 점수순 유지
- **다운로드 0회 유지**(P4 DoD): 취업패키지 서류 원본도 전부 `Content-Disposition: inline`
- 회귀: `run_p2` 48/48 · `run_p3` 109/109 · `run_p4` 165/165 전부 통과 유지
- `tsc -b` · `oxlint` · `vite build` 무경고 (exit 0)
- 검증 스크립트는 DB·storage를 임시 경로로 격리 — `demo/demo.db`·`demo/storage` mtime 변화 없음
- `git status`에 `demo/` 외 신규·수정 파일 없음

**P5에서 남긴 것**
- 취업패키지 **신청서·자가진단 서식 원본이 사업계획서에 없다.** 항목 구성은 데모가 정한 것이고 화면(`FormJobApplication` 안내문·자가진단 각주)과 코드(`ProgramConfig.notes`) 양쪽에 `(데모 추정치)` 라벨을 달았다. TF에서 실제 서식을 받으면 서식1처럼 원본 표 구조로 교체한다
- 결제영수증 판독 금액(`결제금액`)과 신청자가 입력한 영수증 금액의 **교차 검증은 하지 않는다.** 지급액은 입력값 기준으로 계산되고, 판독 금액은 담당자 화면에 근거로만 보인다
- 후순위자 **자동 승계**는 구현하지 않았다. 보완 기한 만료를 화면에 표시하는 데까지다 — 승계는 담당자 판단(승인/반려) 뒤에 오는 운영 절차라 데모 범위 밖으로 둔다

---

## P6. 시연 준비 (D+19 ~ D+21)

- [x] fixture 10종 + 기대 결과 JSON (**더미 데이터만**, R7.1)
- [x] `reset.py` — DB·storage 초기화
- [x] 시연 대본 (클릭 순서, 소요시간, 말할 포인트)
- [x] `demo/README.md` — 기동 방법
- [x] 가정값 라벨 점검 — `(데모 추정치)` 표기가 화면·코드에 모두 있는가

**게이트**: ✅ **통과 (2026-09-16)**
- `python -m demo.backend.tests.run_p6_scenarios` — **249개 검증 항목 전부 통과 (실패 0)**.
  같은 스크립트를 `--live`(진짜 `demo/` 폴더 리셋)로 돌려도 **249 / 0**
- **리셋 → S1~S10 연속 → 기대값 대조**를 한 번의 실행에서 **두 바퀴** 돈다.
  1회차와 2회차 결과가 **신청번호까지 글자 하나 다르지 않다**(S1~S10 전건 `재현: True`).
  시연 중 리셋이 실제로 동작한다는 것을 이걸로 증명한다
- 리셋이 실제로 지우는지부터 본다 — 찌꺼기 1건을 먼저 만들고 리셋 → `지운 대상
  ['demo.db', 'storage']`, 남은 파일 0. 실제 `demo/` 리셋 실측: **19개 파일 · 78.5 MB · 0.01초**
- `reset.py`는 **파괴적 스크립트**라 방어 코드를 세 겹 둔다. ① 대상 폴더에
  `.git`·`engine`·`dashboard`가 있으면 저장소 루트로 보고 거부(`--root .` 로 실제 확인,
  종료코드 2) ② 이름이 `demo.db`/`storage`가 아니면 거부 ③ 직속 자식·비심볼릭 링크만.
  `--dry-run`으로 먼저 목록을 보여주고, `--yes` 없이는 `y` 입력을 요구한다.
  비대화형에서 `--yes`가 없으면 아무것도 지우지 않고 중단
- 리셋 후 `git status`에 **삭제된 추적 파일 0건** (`demo/storage/`·`demo/**/*.db`가
  이미 `.gitignore` 대상). `demo/fixtures/samples/`·소스·문서는 그대로 남는다
- **기대 결과 JSON 10종**을 `demo/fixtures/expected/S1~S10.json`에 뒀다. 시나리오 제목·
  증명하려는 것·클릭 순서·쓰는 파일·**기대값**·할 말이 한 파일에 있고, 검증 스크립트와
  시연 대본이 같은 파일을 본다. 기대값은 리터럴 외에 `$contains`·`$lt`·`$not_empty`
  같은 비교 연산자를 쓴다 (안내 문구 전문·실행마다 달라지는 소요시간용)
- **시연 동선 = 검증 순서**로 맞췄다. S2 신청서를 채운 뒤 **같은 건의 초본 슬롯에서
  S3~S6(미비 4종)을 재업로드로 시연**하고, 올바른 파일로 교체해 제출한다.
  신청번호가 `DS-2026-000001`(S1) → `000002`(S2·S3~S6) → `000003`(S7) → `000004`(S8)
  → `JP-2026-000005`(S10)로 매번 같게 찍힌다
- 시연 대본 `demo/docs/demo-script.md` — 시연 전 점검 6줄, 여는 말, S1~S10 클릭 순서와
  읽을 문구, 닫는 말, **사고 대응표**(파일명이 바뀌었을 때 / 리셋을 빼먹었을 때 /
  Windows 파일 잠김 / 한글 깨짐 / "실제 OCR인가" 질문). 분 단위 시간은 **추정치라고
  명시**했고, 실측한 숫자는 하나뿐임을 밝혔다 — 10종 전체의 서버 처리 시간 **약 2초**
- `demo/README.md` — 폴더 구조, 준비(Python 3.14.0 · Node 22.19.0 실측 환경),
  기동(uvicorn은 **저장소 루트에서**), 리셋, 검증 스크립트 5종, 알아 둘 것 4가지
- 회귀: `run_p2` 48/48 · `run_p3` 109/109 · `run_p4` 165/165 · `run_p5` 146/146 전부 통과 유지
- `tsc -b` · `oxlint` 무경고 (exit 0) · `vite build` 성공
  (pdfjs-dist 때문에 뜨는 chunk 500 kB 경고는 P4부터 있던 것으로 그대로 남는다)
- `git status`에 `demo/` 외 신규·수정 파일 없음

**P6에서 메운 구멍 — 가정값 라벨**
- 판독 신뢰도 0.85 · 화질 0.6 컷라인(PRD Open Question 5)이 **코드 주석에만** 있고
  화면에 없었다. `rules/doc_check.THRESHOLD_NOTES`를 만들어 체크리스트 API로 내보내고
  업로드 화면 각주로 띄웠다
- **선착순 판정 기준 시각**(Open Question 8)이 어디에도 없었다. 데모는 제출 시각
  기준이라는 사실을 `ProgramConfig.notes`에 넣어 사업 선택 화면에 띄운다
- 서식5 **행정정보 코드 불일치**(공고문 371 ↔ 시행지침 46+388, Open Question 4)와
  **전자서명 대체**(Open Question 2)가 코드 주석에만 있었다. 서식 표 바깥에 데모 주석
  구획을 만들어 화면에도 띄웠다 — 원문과 섞이지 않게 표 밖에 둔다

**P6에서 남긴 것**
- 시연 대본의 **분 단위 소요시간은 실측이 아니다.** 사람이 클릭하는 시간이라 리허설을
  해야 나온다. 추정치임을 문서에 명시했고, 실측 후 숫자만 고치면 된다
- `run_p6_scenarios`는 API 수준 재현이다. **브라우저 클릭 자동화(E2E)는 하지 않았다** —
  Playwright 의존성을 데모에 들이는 것보다, 대본대로 사람이 한 번 훑는 편이 낫다고 봤다.
  다만 "다운로드 0회"는 응답 헤더로 기계가 검증한다
- fixture는 **12종 파일 · 25개 더미 PDF**로 시나리오 10종을 덮는다. 더미 PDF는
  `.gitignore` 대상이라 커밋되지 않고 `make_samples.py`로 언제든 다시 만든다

---

## P7. 작성 서식 PDF 내보내기 (D+21 ~, **선택**)

> **메인 기능이 아니다.** P6까지 끝나고 시간이 남을 때만 착수한다. 미구현이어도 DoD에 영향이 없다.

- [x] 원본 PDF에서 서식1·서식5를 **논리 쪽 단위로 잘라** `fixtures/templates/`에 저장 (⚠️ 2-up)
- [x] `fixtures/form_coords/서식1.json`, `서식5.json` 좌표 맵 작성
- [x] `backend/forms/render.py` — PyMuPDF `insert_text()` / `insert_image()` 오버레이
  - [x] `type: "check"` — 체크박스 좌표에 `✓`
  - [x] `type: "image"` — 서식5 서명란에 캔버스 PNG 합성 (R9.2)
- [x] `GET /api/applications/{id}/forms/{form_no}.pdf` — 인라인 응답 (R9.3)
- [x] 담당자 심사 화면 서류 탭에 **"작성 서식" 탭** 추가 — 업로드 서류와 나란히 브라우저에서 확인

**게이트**: ✅ **통과 (2026-09-16)**
- `python -m demo.backend.tests.run_p7_scenarios` — **62개 검증 항목 전부 통과 (실패 0)**
- **좌표를 손으로 적지 않았다.** 템플릿 PDF에서 ① 표 선(`page.get_drawings()`의
  획 도형) ② 글자 원점(`page.get_text("rawdict")`의 `origin`) 두 가지만으로 뽑는다.
  사람이 적은 것은 **원문에 있는 앵커 문구**와 여백 3pt뿐이다. 검증 스크립트가
  좌표를 **다시 뽑아 커밋된 JSON과 통째로 대조**하므로, 손으로 고치면 깨진다
- 뽑힌 값이 P1에서 쓴 열 너비 비율과 맞물린다 — 신청자이름 칸 99.6~151.2pt는 표
  폭(41~381.2) 기준 **17.2% ~ 32.4%**, 생년월일 칸은 **49.2% ~ 66.1%**. 설계 6-A.5의
  경계값과 같은 자리다
- **왕복 검증**: 서식1 글자 19종·체크 49종, 서식5 글자 5종·체크 2종·이미지 1종을
  좌표에 얹은 뒤 PDF를 다시 열어 **그 칸 안에서 글자로 되읽었다**. 서식1 14개 값 ·
  서식5 5개 값 전부 일치. 칸 밖으로 나간 글자 0건
- **겹침은 픽셀로 쟀다.** 템플릿과 내보낸 서식을 150dpi로 나란히 그려 맞대고,
  달라진 픽셀 중 **원본이 이미 검던 자리**를 센다. 서식1 얹은 잉크 **8,242px 중 0px**,
  서식5 **2,342px 중 0px**. 체크칸·서명란은 일부러 겹치는 자리라 제외
- **체크는 체크한 곳에만**: 서식1 p0 10개 · p1 4개가 정확히 그 `□` 좌표에 찍히고,
  체크 안 한 35개 보기에는 `✓`가 0건. 거주기간·근로기간은 날짜에서 자동으로 나온다
  (2021-11-20 전입 → **4년 이상 ~ 5년 미만**, 2023-01-02 취업 → **3년 이상**)
- **전자서명 합성**(R9.2): 서식5에 이미지 객체 1개, 배치 좌표
  `(290.1, 421.9)-(352.2, 446.8)`가 서명란 상자 안. 원문 "(서명 또는 인)" 자리다 —
  종이에 도장을 찍는 그 자리. 동의하지 않은 건은 이미지 0개 + '동의하지 않음'에 체크
- **다운로드 0회 유지**: 내보내기 응답도 전부 `Content-Disposition: inline`,
  내려받기 지시 0건. 담당자 화면의 서식 탭 2개도 같은 값. P4 정적 검사(금칙어)도 통과
- 템플릿은 원본에서 **필요한 논리 쪽만** 잘라 폰트를 부분집합화했다 —
  서식1 2쪽 **166KB**, 서식5 1쪽 **92KB**. 자른 쪽이 맞는지 쪽번호(`- 23 -`/`- 24 -`/
  `- 28 -`)로 검증한다. 루트 `.gitignore`의 `*.pdf`에서 이 둘만 되살렸다
- 회귀: `run_p2` 48/48 · `run_p3` 109/109 · `run_p4` 165/165 · `run_p5` 146/146 ·
  `run_p6` 249/249 전부 통과 유지
- `tsc -b` · `oxlint` · `vite build` 무경고 (exit 0. pdfjs chunk 500kB 경고는 P4부터 있던 것)
- 검증 스크립트는 DB·storage를 임시 경로로 격리 — 실물 `demo/` 오염 없음
- `git status`에 `demo/` 외 신규·수정 파일 없음 (예외: 루트 `.gitignore` 1줄)

**P7에서 남긴 것**
- **서식2~4는 좌표 맵 구조만** 남겼다(설계 6-B.3의 범위 그대로). 원본 위치
  (물리 쪽·좌우 반쪽·쪽번호)까지 JSON에 적어 뒀으므로, 채우려면 `cut_templates.py`에
  한 줄, `build_form_coords.py`에 앵커 규격을 더하면 된다
- **취업지원패키지는 내보낼 서식이 없다.** 사업계획서에 원본 서식 자체가 없어
  (P5에서 남긴 것) 목록이 빈 배열로 나가고 담당자 화면에 탭이 뜨지 않는다
- 서식1의 **유사 자산형성사업 상세(사업명·기간·수령액)** 칸은 좌표만 뽑아 뒀고
  화면에 입력란이 없어 실제로는 늘 빈칸이다. 화면 쪽 항목이 생기면 그대로 찍힌다
- 전자서명은 원문 **"(서명 또는 인)" 글자 위에 겹쳐** 찍는다. 빈칸이 52pt뿐이라
  성명과 나란히 놓을 수 없었고, 배경이 투명한 PNG라 원문이 비쳐 보인다. 종이에
  도장을 찍는 것과 같은 모양이라 이 편이 자연스럽다고 봤다
- **템플릿 재생성에는 원본 시행지침 PDF가 필요하다**(루트 `*.pdf`는 커밋 대상이
  아니다). 템플릿·좌표 맵은 커밋되므로 평소에는 필요 없고, 원본이 개정될 때만
  두 스크립트를 다시 돌린다

---

---

## 이 태스크의 시연 시나리오

| # | 시나리오 | 증명하는 것 |
|---|---|---|
| S10 | 취업패키지 면접비+자격증 복수 신청 | 항목별 체크리스트 조립 + 실비 한도 계산 + 7일 보완 안내 |

P6에서 S1~S10 **전체 10종**의 fixture와 무중단 재현을 책임진다.

---

## Definition of Done

- [x] 취업패키지 복수 항목 신청과 실비 계산이 동작한다 (정장 70,000원 → 50,000원) — P5 게이트
- [x] 선착순 표시와 7일 보완 기한 카운트다운이 보인다 — P5 게이트
- [x] 리셋 후 **10개 시연 시나리오가 무중단 재현**된다 — `run_p6_scenarios` 249/0, 2회 연속 동일
- [x] 미확정 가정값이 화면과 코드에 `(데모 추정치)` 라벨로 명시되어 있다 — 7개 항목 대조 완료
- [x] `demo/README.md`에 기동 방법이 있다

**선택 (P7 — 미구현이어도 DoD 충족)**

- [x] 서식1·서식5를 작성된 상태 그대로 PDF로 내보낼 수 있다 — P7 게이트
- [x] 서식5 PDF의 서명란에 전자서명이 합성되어 있다 — P7 게이트
